bin/
# `go build` without -o drops the binary here.
desktop
# Generated by `make frontend`, and embedded by main.go. `go build` needs it to
# exist, so build the frontend before building the app.
frontend/dist/
